╔═══════════════════════════════════════════════════════════════════════════════╗
║                    LINEAR TREND SPOTTER - TECHNICAL SPEC                      ║
║                    Automated Crypto Trend Detection System                    ║
╚═══════════════════════════════════════════════════════════════════════════════╝


┌─────────────────────────────────────────────────────────────────────────────┐
│                          SYSTEM ARCHITECTURE                                │
└─────────────────────────────────────────────────────────────────────────────┘

Language: Python 3.13
Host: PythonAnywhere (scheduled tasks)
Database: SQLite3 (local)
Notification: Telegram Bot API
External APIs: CoinMarketCap, CoinGecko, CoinLore, Chart-IMG


┌─────────────────────────────────────────────────────────────────────────────┐
│                            DATA SOURCES & API USAGE                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ COINMARKETCAP (PRO API)                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│ Endpoint: /v1/cryptocurrency/listings/latest
│ Calls per scan: 1 (single batch request)
│ Data retrieved: 7d, 30d, 60d, 90d % changes, 24h volume, rank
│ Limit: 2500 coins per request (configured)
│ Rate limit: 30/min, 2s min interval
│
│ └─ Returns raw market data for bulk processing
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ COINGECKO (Free Tier)                                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│ Endpoint 1: /coins/{id}/tickers
│   Purpose: Exchange-specific volume data (Coinbase, Kraken, MEXC)
│   Calls: ~100-200 per scan (after filtering)
│
│ Endpoint 2: /coins/{id}/market_chart
│   Purpose: 30-day OHLCV data for uniformity calculation
│   Calls: ~20-40 per scan (after filtering)
│
│ Endpoint 3: /coins/list (one-time mapping build)
│   Purpose: Build local symbol → ID mapping database
│
│ Rate limits: 30-50/min, 12s base interval + exponential backoff
│ Cache: 24h for tickers, 6h for price history
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ COINLORE (Free, No API Key)                                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│ Endpoint: /api/ticker/?id=ID1,ID2,ID3
│ Purpose: Secondary validation, backup data source
│ Calls: Batch processing, 100 IDs per request
│ Rate limit: 1/s recommendation (respected)
│
│ └─ No rate limits, 14k+ coins, used for cross-validation
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ CHART-IMG (API Key)                                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│ Endpoint: /v2/tradingview/advanced-chart
│ Purpose: Generate professional TradingView-style charts
│ Calls: 1 per qualified coin (~20 per scan)
│ Rate limit: 1/s (queued)
│
│ └─ Returns PNG images attached to Telegram notifications
└─────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                         FILTERING PIPELINE (STAGES)                         │
└─────────────────────────────────────────────────────────────────────────────┘

INPUT: Exchange listings database (Coinbase + Kraken + MEXC)
       Built from exchange APIs, updated weekly
       ~2,000-5,000 unique symbols

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 1: COINMARKETCAP BATCH FETCH                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ Call CMC once for top 2500 coins
│ Build hash map: symbol → {gains, volume, metadata}
│
│ Complexity: O(n) memory, O(1) API call
│ API Calls: 1
└─────────────────────────────────────────────────────────────────────────────┘

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 2: VOLUME FILTER                                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│ threshold = config.MIN_VOLUME_M (default 1e6 USD)
│
│ for coin in exchange_symbols:
│     if coin in cmc_data and cmc_data[coin].volume_24h >= threshold:
│         keep()
│
│ Selectivity: ~30% pass (750/2500)
│ Complexity: O(n) hash lookup
│ API Calls: 0
└─────────────────────────────────────────────────────────────────────────────┘

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 3: GAIN FILTER (7d & 30d)                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ Require: gain_7d > 7% AND gain_30d > 30%
│ Rationale: >1% avg daily growth over both periods
│
│ if coin.gains['7d'] > 7 and coin.gains['30d'] > 30:
│     keep()
│
│ Selectivity: ~15% of volume-pass (112/750)
│ Complexity: O(n) arithmetic
│ API Calls: 0
└─────────────────────────────────────────────────────────────────────────────┘

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 4: EXCHANGE LISTING VERIFICATION                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│ Local DB lookup against exchange_listings table
│ Verify coin is actually tradable on target exchanges
│
│ for coin in gain_qualified:
│     coin.listed_on = [ex for ex in TARGET_EXCHANGES 
│                       if exchange_db.is_listed(coin.symbol, ex)]
│
│ Selectivity: ~90% pass (100/112)
│ Complexity: O(n * 3) DB lookups
│ API Calls: 0
└─────────────────────────────────────────────────────────────────────────────┘

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 5: COINGECKO ID RESOLUTION                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│ Local mapping DB (built from /coins/list)
│ Map symbol → CoinGecko API ID (e.g., "BTC" → "bitcoin")
│
│ for coin in exchange_qualified:
│     coin.cg_id = mapping_db.get_id(coin.symbol)
│
│ Pass rate: ~90% (varies by coin obscurity)
│ Complexity: O(n) indexed lookup
│ API Calls: 0
└─────────────────────────────────────────────────────────────────────────────┘

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 6: EXCHANGE VOLUME FETCHING (COINGECKO)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│ Rate-limited batch requests to /tickers endpoint
│ Extract volumes for Coinbase, Kraken, MEXC
│
│ for coin in coins_with_ids:
│     tickers = gecko.get_tickers(coin.cg_id)
│     coin.volumes = parse_exchange_volumes(tickers)
│
│ Rate limiting: 12s base + exponential backoff on 429
│ API Calls: 100-200 per scan
│ Caching: 24h
└─────────────────────────────────────────────────────────────────────────────┘

↓


┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 7: UNIFORMITY SCORE CALCULATION (CORE ALGORITHM)                      │
├─────────────────────────────────────────────────────────────────────────────┤
│ Input: 30 daily closing prices (oldest to newest)
│ Output: score ∈ [0,100]
│
│ ALGORITHM:
│
│ 1. Normalize to cumulative percentage change
│    base = prices[0]
│    cum_pct[i] = ((prices[i] - base) / base) * 100
│
│ 2. Calculate ideal uniform line
│    total_gain = cum_pct[29]
│    daily_gain = total_gain / 29
│    ideal[i] = i * daily_gain
│
│ 3. Calculate deviation
│    total_deviation = Σ |cum_pct[i] - ideal[i]|
│
│ 4. Calculate max possible deviation (worst case)
│    max_dev = Σ total_gain for i < 29
│
│ 5. Normalize and transform
│    norm_dev = total_deviation / max_dev
│    raw_score = 100 * (1 - √(min(norm_dev, 1)))
│    score = clamp(raw_score, 0, 100)
│
│ INTERPRETATION:
│   ≥90: Perfectly uniform
│   ≥75: Very uniform
│   ≥60: Good uniformity
│   ≥45: Fair (configurable threshold)
│   <45: Poor (likely "hockey stick")
│
│ Complexity: O(n) with n=30
│ API Calls: ~20-40 (cached 6h)
└─────────────────────────────────────────────────────────────────────────────┘

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 8: UNIFORMITY FILTER                                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│ threshold = config.UNIFORMITY_MIN_SCORE (default 45)
│
│ if coin.uniformity_score >= threshold and coin.total_gain > 0:
│     final_qualified.append(coin)
│
│ Selectivity: ~20% of previous stage (20/100)
│ Complexity: O(n)
│ API Calls: 0
└─────────────────────────────────────────────────────────────────────────────┘

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 9: ENTRY/EXIT DETECTION                                               │
├─────────────────────────────────────────────────────────────────────────────┤
│ Compare current qualified list against active_coins table
│
│ entered = current - active
│ exited = active - current
│
│ for coin in entered:
│     active_db.add(coin)      # Store with timestamp
│     send_notification(coin)   # New entry alert
│
│ for coin in exited:
│     active_db.remove(coin)    # Clear from tracking
│     send_exit_notification(coin)
│
│ Complexity: O(n) set operations
│ API Calls: 0
└─────────────────────────────────────────────────────────────────────────────┘

↓

┌─────────────────────────────────────────────────────────────────────────────┐
│ STAGE 10: NOTIFICATION DELIVERY (TELEGRAM)                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│ For each entered coin:
│
│ 1. Fetch chart image from Chart-IMG (rate-limited)
│    chart_bytes = chart_img.get_chart(coin.symbol)
│
│ 2. Format message with HTML:
│    caption = f"""
│    🟢 <a href='{cmc_url}'>{symbol} ({name})</a>
│    
│    📊 Gains:
│       7d: +{gain_7d:.1f}%
│       30d: +{gain_30d:.1f}%
│    
│    📈 Uniformity Score: {score}/100
│    
│    💰 Exchange Volumes:
│    🟦 Coinbase: ${vol_coinbase:,.0f}
│    🐙 Kraken: ${vol_kraken:,.0f}
│    🟪 MEXC: ${vol_mexc:,.0f}
│    """
│
│ 3. Send via Telegram Bot API
│    telegram.send_photo(img_data, caption=caption)
│
│ For exited coins:
│    telegram.send_message(f"🔴 {symbol} has left the qualified list")
│
│ API Calls: 1 per qualified coin (Chart-IMG)
└─────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                       PERFORMANCE CHARACTERISTICS                           │
└─────────────────────────────────────────────────────────────────────────────┘

Input: 2,500 coins from CMC + exchange listings
Output: 15-25 qualified coins per scan

API Call Distribution:
├─ CoinMarketCap: 1
├─ CoinGecko (tickers): ~100-200
├─ CoinGecko (price): ~20-40 (cached 6h)
├─ Chart-IMG: ~15-25
└─ Total per scan: ~150-250 calls


Filter Selectivity:
├─ Volume filter: 70% eliminated
├─ Gain filter: 85% of remaining eliminated
├─ Uniformity filter: 80% of remaining eliminated
└─ Overall: ~99.2% elimination rate → 0.8% pass rate

Time per scan: 10-15 minutes (rate-limit bound)
Cache effectiveness: 60-80% hit rate after first scan


┌─────────────────────────────────────────────────────────────────────────────┐
│                          RATE LIMIT MANAGEMENT                              │
└─────────────────────────────────────────────────────────────────────────────┘

class RateLimiter:
    """Exponential backoff with jitter"""
    
    def wait_if_needed(self):
        base_wait = max(0, self.min_interval - time_since_last)
        backoff = min(2 ** self.consecutive_429s, 300)
        total_wait = max(base_wait, backoff)
        time.sleep(total_wait + random.uniform(0, 0.1))

Backoff strategy:
├─ First 429: wait 60s
├─ Second 429: wait 120s
├─ Third 429: wait 240s
└─ Max backoff: 300s

Cache durations:
├─ CoinGecko mappings: 30 days
├─ Exchange volumes: 24h
├─ Price history: 6h
└─ Coin list: 24h


┌─────────────────────────────────────────────────────────────────────────────┐
│                          DATABASE SCHEMA (SQLite)                           │
└─────────────────────────────────────────────────────────────────────────────┘

TABLE coin_list (
    id TEXT PRIMARY KEY,           -- CoinLore ID
    symbol TEXT,                    -- Trading symbol (lowercase)
    name TEXT,                      -- Full name
    rank INTEGER,                   -- Market cap rank
    last_updated TEXT                -- ISO timestamp
)
INDEX idx_coin_list_symbol ON coin_list(symbol)

TABLE price_cache (
    coin_id TEXT PRIMARY KEY,       -- CoinGecko ID
    prices TEXT,                    -- JSON array of 30 prices
    uniformity_score REAL,
    gains_30d REAL,
    gains_60d REAL,
    gains_90d REAL,
    cache_date TEXT
)

TABLE active_coins (
    coin_symbol TEXT,
    coin_name TEXT,
    gecko_id TEXT,
    coinlore_id TEXT,
    entered_date TEXT,
    last_seen_date TEXT,
    last_scan_date TEXT,
    gain_7d REAL,
    gain_30d REAL,
    norm_slope REAL,
    coinbase_volume TEXT,
    kraken_volume TEXT,
    mexc_volume TEXT,
    slug TEXT,
    cmc_url TEXT,
    PRIMARY KEY (coin_symbol, coin_name)
)

TABLE scan_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    scan_date TEXT,
    coin_name TEXT,
    coin_symbol TEXT,
    gain_7d REAL,
    gain_30d REAL,
    norm_slope REAL,
    coinbase_volume TEXT,
    kraken_volume TEXT,
    mexc_volume TEXT,
    cmc_url TEXT
)


┌─────────────────────────────────────────────────────────────────────────────┐
│                         CONFIGURATION PARAMETERS                            │
└─────────────────────────────────────────────────────────────────────────────┘

{
    "CMC_API_KEY": "...",                    // CoinMarketCap Pro API
    "TELEGRAM_BOT_TOKEN": "...",              // Bot token
    "TELEGRAM_CHAT_ID": "...",                // Target group ID
    "CHART_IMG_API_KEY": "...",               // Chart-IMG API key
    "MIN_VOLUME_M": 1000000,                  // Minimum 24h volume
    "TARGET_EXCHANGES": ["coinbase", "kraken", "mexc"],
    "UNIFORMITY_MIN_SCORE": 45,                // Minimum acceptable score
    "UNIFORMITY_PERIOD": 30,                   // Days to analyze
    "RETRY_MAX_ATTEMPTS": 3,                   // API retry attempts
    "RETRY_DELAY": 2,                          // Initial retry delay
    "RETRY_BACKOFF": 2                          // Exponential backoff factor
}


┌─────────────────────────────────────────────────────────────────────────────┐
│                        DEPLOYMENT & SCHEDULING                              │
└─────────────────────────────────────────────────────────────────────────────┘

Platform: PythonAnywhere (paid account for always-on)
Scheduled tasks (cron):


55 * * * * /home/user/mysite/run_scanner.sh          # Hourly scan
0 0 * * 0 /home/user/mysite/update_exchanges.py      # Weekly exchange update
0 0 1 * * /home/user/mysite/update_mappings.py       # Monthly mapping refresh
*/5 * * * * /home/user/mysite/bot_watchdog.py        # Bot uptime monitoring

Processes:
├─ scheduler.py: Main cron launcher with file locking
├─ telegram_bot.py: Long-running bot process (background)
├─ manage_bot.py: Bot process manager (start/stop/restart)
└─ watchdog.py: Auto-restart on failure


┌─────────────────────────────────────────────────────────────────────────────┐
│                          ERROR HANDLING & LOGGING                           │
└─────────────────────────────────────────────────────────────────────────────┘

Log files:
├─ trend_scanner.log: Main application log (rotated)
├─ bot_output.log: Telegram bot output
├─ cron_output.log: Scheduled task output
├─ exchange_update.log: Exchange listing updates
└─ mapping_update.log: CoinGecko mapping updates

Error recovery:
├─ API 429: Exponential backoff with jitter
├─ Database locked: Retry up to 3 times with 100ms delay
├─ Network timeout: Retry with increasing delays
└─ Process crash: Watchdog auto-restart within 5 minutes


┌─────────────────────────────────────────────────────────────────────────────┐
│                         PERFORMANCE OPTIMIZATIONS                           │
└─────────────────────────────────────────────────────────────────────────────┘

1. Batch processing
   ├─ 50 coins per CoinGecko ticker request
   └─ 100 coins per CoinLore batch request

2. Local caching
   ├─ Coin list: 24h refresh
   ├─ Price history: 6h cache
   ├─ Exchange volumes: 24h cache
   └─ Symbol→ID mapping: 30d cache

3. Filter ordering
   ├─ Most selective filters FIRST (exchange check, volume)
   ├─ Most expensive filters LAST (uniformity)
   └─ Progressive reduction: 2500 → 100 → 20

4. Connection pooling
   ├─ Reuse HTTP sessions
   ├─ SQLite WAL mode for concurrency
   └─ Thread-local connections

5. Memory efficiency
   ├─ Streaming JSON parsing where possible
   ├─ Generator expressions for large lists
   └─ LRU cache for frequently accessed data
